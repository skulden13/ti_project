import { counterReducer } from './model/slice/couterSlice';
import type { CounterSchema } from './model/types/counterSchema';
import { Counter } from './ui/Counter';

export { counterReducer, CounterSchema, Counter };
